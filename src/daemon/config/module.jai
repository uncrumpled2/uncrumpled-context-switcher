// Config module - exports all configuration-related functionality
//
// Note: validator.jai is NOT loaded here because it depends on Context type
// from context_store.jai. Load it separately after context_store is available.

#load "parser.jai";
